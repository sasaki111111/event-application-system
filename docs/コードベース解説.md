# コードベース解説

このシステムの「frontend」「backend」「API」がそれぞれ何なのか、フォルダ・ファイル単位でどう役割分担しているのかをまとめたもの。公式の設計書（`docs/design/`）とは別に、コードを読むための補助資料として作成。

---

## 1. frontend・backend・APIの関係

### 1.1 全体像

```
【ブラウザ】
   ↓ 画面を表示
frontend（Angular、localhost:4200）
   ↓ データが欲しいときにAPIで問い合わせ
backend（Spring Boot、localhost:8080）
   ↓ データの読み書き
MySQL（eventappデータベース）
```

- frontendとbackendは**別々に起動する、別々のプログラム**（同じPC上で違う「部屋番号＝ポート」で動いている）
- frontendは画面（見た目）だけを担当し、データそのものは持っていない
- backendはデータ（DB経由）だけを担当し、画面は一切持っていない
- 両者をつなぐ「共通の言葉のルール」が **API**

### 1.2 例え

| 例え役 | 実際の役割 |
|---|---|
| 店員（frontend） | お客さん（人間）に見せる盛り付け・接客をする。食材は持っていない |
| 厨房（backend） | 食材（データ）を用意するだけ。盛り付けや接客はしない |
| 伝票（API） | 店員と厨房の間の「◯◯をください」「はいこれ」というやり取りの共通フォーマット |

### 1.3 APIとは何か（もう少し具体的に）

APIは、frontendとbackend**両方に実装されている、一致していなければならない約束事**。`docs/design/API設計書_v1.1.md`で決めた内容が、実際には両側にこう対応している。

| 決めごと | frontend側の実装 | backend側の実装 |
|---|---|---|
| どのURLに聞くか | `core/event-api.ts` | `controller/EventController.java`の`@GetMapping` |
| 返ってくるデータの形 | `interface EventSummary {...}` | `dto/EventSummaryResponse.java` |
| 認証方法 | `login/login.ts`（メールアドレス入力）→`core/login-api.ts`（`/api/login`でロール判定、`/api/users`で新規登録）→`core/dummy-user-store.ts`（保持）→`core/dummy-auth-interceptor.ts`（`X-User-Id`ヘッダを付与） | `common/AuthInterceptor.java`（同ヘッダを検証）・`controller/UserController.java`（`/api/login`・`/api/users`） |
| エラーの形 | （受け取る側） | `common/exception/ErrorResponse.java` |

片方だけ変更するとズレて壊れるため、常にペアで考える必要がある。

---

## 2. フォルダ・ファイルの役割（詳細）

### 2.1 backend（`backend/src/main/java/com/example/eventapp/`）

Controller → Service → Repository という3層構造で、データがDBに近い方から遠い方へ流れる。

#### `entity/` — DBのテーブル1行をJavaのクラスとして表現

| ファイル | まとめられている内容 |
|---|---|
| `User.java` | `users`テーブル1行分（id・name・email・role等）。会員登録（機能追加）用のコンストラクタも持つ |
| `Event.java` | `events`テーブル1行分。「今受付中か」を判定する`isOpen()`も持つ |
| `Application.java` | `applications`テーブル1行分。`User`・`Event`への参照を持つ |
| `ApplicationStatus.java` | `"受付済"`/`"キャンセル済"`という文字列を定数化（あちこちに直書きしないため） |

#### `repository/` — DBへの問い合わせ窓口

| ファイル | まとめられている内容 |
|---|---|
| `UserRepository.java` | `users`テーブルの検索（`AuthInterceptor`がログインユーザー確認に使う）。メール検索・重複チェック・一覧取得も持つ |
| `EventRepository.java` | `events`テーブルの検索（開催日時順の一覧取得など） |
| `ApplicationRepository.java` | `applications`テーブルの検索（受付済数のカウント、二重申込チェック用の存在確認など） |
| `package-info.java` | このフォルダ自体の役割説明（実行されるコードではない） |

中身はほぼ空だが、Spring Data JPAがメソッド名から自動でSQLを生成して実装してくれる。

#### `service/` — 業務ロジック（計算・判断）

| ファイル | まとめられている内容 |
|---|---|
| `EventService.java` | イベントの一覧・詳細・登録・編集・削除。`acceptedCount`・`open`・`remaining`の計算、削除時の「申込があれば拒否」判定 |
| `ApplicationService.java` | イベント申込。定員超過・締切超過・二重申込のチェックを行ってから申込データを作る |
| `UserService.java` | メールアドレスでのログイン・軽い会員登録・ユーザー一覧（いずれも機能追加）。パスワードは扱わない |
| `PingService.java` | 起動確認用の最小サンプル（`"pong"`を返すだけ） |

#### `dto/` — APIでやり取りするJSONの形の定義

| ファイル | まとめられている内容 |
|---|---|
| `EventSummaryResponse.java` | イベント一覧APIが返す1件分の形 |
| `EventDetailResponse.java` | イベント詳細・登録・編集APIが返す形（一覧の項目＋説明文・残り枠） |
| `EventUpsertRequest.java` | イベント登録・編集APIが受け取る入力の形（入力チェック付き） |
| `ApplicationCreateRequest.java` | イベント申込APIが受け取る入力の形（イベントIDのみ） |
| `ApplicationResponse.java` | イベント申込APIが返す形（申込ID・ステータス・申込日時等） |
| `LoginRequest.java` | ログインAPIが受け取る入力の形（メールアドレスのみ、機能追加） |
| `UserRegisterRequest.java` | 会員登録APIが受け取る入力の形（名前・メールアドレスのみ、機能追加） |
| `UserResponse.java` | ログイン・会員登録・ユーザー一覧APIが返す形（userId・name・email・role、機能追加） |

DBの`Event`/`Application`エンティティをそのまま返さず、必要な項目だけに変換して渡すためのもの。

#### `controller/` — HTTPリクエストの入口

| ファイル | まとめられている内容 |
|---|---|
| `EventController.java` | `GET`（一覧・詳細）・`POST`（登録）・`PUT`（編集）・`DELETE`（削除） `/api/events`系。登録・編集・削除は管理者のみ |
| `ApplicationController.java` | `POST /api/applications`（イベント申込） |
| `UserController.java` | `POST /api/login`（メールアドレスでログイン）・`POST /api/users`（会員登録）・`GET /api/users`（ユーザー一覧、管理者専用）。login・registerは認証不要（機能追加） |
| `PingController.java` | `GET /api/ping`（起動確認）・`GET /api/whoami`（認証確認、開発用） |

#### `common/` — 全APIに共通の仕組み

| ファイル | まとめられている内容 |
|---|---|
| `AuthInterceptor.java` | `X-User-Id`ヘッダを検証する「関所」。Controllerに届く前に必ず通る |
| `AuthContext.java` | 検証済みのログインユーザー情報を、そのリクエストの間だけ保持する入れ物 |
| `CurrentUser.java` | ログインユーザー情報そのもの（id・名前・role） |
| `WebConfig.java` | `AuthInterceptor`をどのURLに適用するか、CORS（frontendからのアクセス許可）の設定 |
| `exception/UnauthorizedException.java` | 401（未認証）を表す例外 |
| `exception/ForbiddenException.java` | 403（権限不足）を表す例外 |
| `exception/NotFoundException.java` | 404（データが見つからない）を表す例外 |
| `exception/BusinessException.java` | 400（業務ルール違反、定員超過など）を表す例外 |
| `exception/ErrorResponse.java` | エラー発生時にJSONとして返す形の定義 |
| `exception/GlobalExceptionHandler.java` | 上記の例外を全部キャッチし、統一されたJSON形式に変換する係 |
| `validation/ValidEventDates.java` | 「申込締切は開催日時以前」という、2つの入力項目にまたがるチェックの定義（独自のバリデーションルール） |
| `validation/EventDatesValidator.java` | 上記の実際のチェック処理本体 |

#### `resources/` — 設定ファイル・DB関連

| ファイル | まとめられている内容 |
|---|---|
| `application.yml` | DBの接続先など、アプリ全体の設定（パスワードなどの秘密情報は書かない） |
| `application-local.yml` | 実際のDBパスワード（**Gitには含まれない、各自のPCにだけ存在**） |
| `db/schema.sql` | テーブルを作成するDDL（CREATE TABLE文） |
| `db/seed.sql` | 初期データ（固定ユーザー2件、サンプルイベント8件。サークル・エンタメ系のテーマは`docs/design/要件定義書vol2_サークルイベント版.md`参照） |

#### `src/test/` — テストコード（G-1・G-2）

| ファイル | まとめられている内容 |
|---|---|
| `service/ApplicationServiceTest.java` | （G-1）`ApplicationService`の申込・キャンセルの業務ロジック（定員超過・受付終了・二重申込・取消可否等）を、Repositoryをモック化してDBに触れずに検証するユニットテスト |
| `service/EventServiceTest.java` | （G-1）`EventService.delete()`の業務ロジック（受付済の申込が残っていれば削除を拒否する）を検証するユニットテスト |
| `ApiIntegrationTest.java` | （G-2）Controller→Service→Repository→H2（実DB）までを実際にHTTPで通す結合テスト。API-01〜06を、TestRestTemplateで実際にリクエストを送りDBの状態も確認しながら検証する |
| `BackendApplicationTests.java` | アプリ全体が起動できるかだけを見る最小限のテスト（Spring Boot生成） |
| `resources/application.yml` | テスト実行時だけ使う設定（実MySQLの代わりにH2インメモリDBを使う） |

> G-2の結合テストを作る過程で、`Event`・`Application`の`created_at`/`updated_at`列がテスト用H2スキーマ（Hibernateがエンティティ定義から自動生成）だとNOT NULLなのにDEFAULT値が無く、INSERTが失敗する問題が見つかった。本番用の`db/schema.sql`には`DEFAULT CURRENT_TIMESTAMP`があるが、Hibernateの自動生成スキーマはそれを知らないため。エンティティの`@Column`に`columnDefinition`を追加して解決した（本番は`ddl-auto: none`のため影響なし）。

---

### 2.2 frontend（`frontend/src/app/`）

Angularでは、1つの画面・機能につき`.ts`（データと動き）・`.html`（画面の構造）・`.css`（見た目）の3点セットで作る。

#### `core/` — API通信など、画面をまたいで共通で使うもの

| ファイル | まとめられている内容 |
|---|---|
| `event-api.ts` | backendの`/api/events`系API（一覧・詳細・登録・編集・削除）を呼び出す窓口。データの型もここで定義 |
| `application-api.ts` | backendの`/api/applications`（イベント申込・マイページ・キャンセル）を呼び出す窓口 |
| `report-api.ts` | backendの`/api/reports/applications`（申込実績のJSON集計・CSVダウンロード）を呼び出す窓口 |
| `user-api.ts` | backendの`/api/users`（GET、管理者専用のユーザー一覧）を呼び出す窓口（機能追加、マスタ確認用） |
| `dummy-auth-interceptor.ts` | 送信する全リクエストに`X-User-Id`ヘッダを自動で付ける仕組み。どのuserIdを付けるかはログイン画面（`login/`）で入力・確認されたIDをDummyUserStoreから読む |
| `login-api.ts` | ログイン画面から呼ぶ認証不要のAPI窓口。`/api/login`（メールアドレスでログイン）・`/api/users`（POST、会員登録）を呼び出す |
| `http-error-interceptor.ts` | 全リクエストのエラーを共通処理する仕組み（E-1）。バックエンドに繋がらない場合（status 0）のメッセージを一箇所にまとめ、各画面は`err.error?.message`を読むだけでよくする |
| `dummy-user-store.ts` | ログイン中のユーザー（ダミー認証のロール）を覚えておく入れ物（E-2）。未ログイン時は`null` |
| `auth-guard.ts` | 未ログイン（`null`）で保護ルートに来た場合に`/login`へ戻すルートガード（E-2） |
| `admin-guard.ts` | `/admin/**`への画面遷移を、管理者以外は`/events`に戻すルートガード（`auth-guard.ts`の後段） |

#### `login/` — SC-01 ログイン画面

| ファイル | まとめられている内容 |
|---|---|
| `login.ts`/`.html`/`.css` | ログイン画面（`/login`）。パスワードは無く、メールアドレスを入力すると`login-api.ts`経由でロールを判定し、一般ユーザー／管理者それぞれの初期画面へ自動的に遷移する（ダミー認証）。名前・メールアドレスだけで一般ユーザーを作れる会員登録欄も持つ |

#### `events/` — イベントに関する画面（一般ユーザー向け）

| フォルダ | まとめられている内容 |
|---|---|
| `event-list/` | イベント一覧画面（`/events`）。`.ts`=一覧取得のロジック、`.html`=表の構造、`.css`=バッジ等の装飾。行を展開（▼）すると詳細取得＋その場で申込もできる |
| `event-search/` | イベント検索画面（`/events/search`、機能追加）。一覧を全件取得し、キーワード・開催日範囲でフロント側のみ絞り込む |
| `event-detail/` | イベント詳細＋申込画面（`/events/:id`）。詳細表示に加えて「申し込む」ボタンの処理を持つ。`?from=search`付きで来た場合は「戻る」の行き先が検索画面になる |
| `apply-done/` | 申込完了画面（`/events/:id/done`）。申込APIのレスポンスをそのまま表示するだけの画面 |

#### `admin/` — 管理者向け画面

| フォルダ | まとめられている内容 |
|---|---|
| `admin-event-list/` | イベント管理一覧画面（`/admin/events`）。一覧表示に加えて編集リンク・削除ボタンを持つ |
| `admin-event-form/` | イベント登録・編集フォーム（`/admin/events/new`・`/admin/events/:id/edit`）。ルートに`:id`があれば編集、無ければ新規登録として動く1つのコンポーネント |
| `admin-report/` | 申込状況・実績画面（`/admin/reports`）。イベント別の受付数・充足率のJSON集計表示とCSVダウンロードボタンを持つ |
| `admin-user-list/` | ユーザー一覧画面（`/admin/users`、機能追加）。登録済みユーザー（ID・名前・メール・ロール）を確認できるマスタ画面 |

#### `my-applications/` — 一般ユーザーのマイページ

| ファイル | まとめられている内容 |
|---|---|
| `my-applications.ts`/`.html`/`.css` | 自分の申込一覧画面（`/my/applications`）。一覧表示に加えて申込のキャンセルボタンを持つ |

#### ルート直下（`src/app/`）— アプリ全体の骨組み

| ファイル | まとめられている内容 |
|---|---|
| `main.ts` | ブラウザが最初に実行するファイル。アプリを起動する |
| `app.ts` / `app.html` / `app.css` | 一番外側の部品。共通ナビ（イベント一覧・イベント管理へのリンク、ユーザー切り替えボタン）＋`router-outlet`（画面の差し込み口） |
| `app.config.ts` | HttpClient・Routerなど、アプリ全体に効く設定をまとめる場所。HttpClientには`dummyAuthInterceptor`（認証ヘッダ付与）と`httpErrorInterceptor`（通信エラーの共通化）を登録 |
| `app.routes.ts` | URL（`/login`・`/events`・`/admin/events`等）とコンポーネントの対応表。保護ルートには`auth-guard.ts`、管理者用ルートにはさらに`admin-guard.ts`を適用 |
| `app.spec.ts` | `app.ts`のテストコード |

#### `src/`直下（`app/`の外）

| ファイル | まとめられている内容 |
|---|---|
| `index.html` | ブラウザが最初に読み込むHTML（中身は`<app-root>`だけ） |
| `styles.css` | アプリ全体に効くグローバルスタイル（現時点では未使用） |

---

### 2.3 リポジトリ全体のルート

| フォルダ/ファイル | まとめられている内容 |
|---|---|
| `docs/` | 設計書（要件定義書・API設計書・テーブル定義書等）とWBS |
| `docs/design/` | フェーズA（要件・設計）の正式な成果物 |
| `backend/` | バックエンド（Spring Boot）のプロジェクト一式 |
| `frontend/` | フロントエンド（Angular）のプロジェクト一式 |
| `.github/` | Issue・PRのテンプレート |
| `README.md` | セットアップ手順 |
| `.gitignore` | Gitで管理しないファイルの一覧 |
