# イベント申込システム 詳細設計書

v2.0（2026-09-18）

要件定義書_v2.0.md・API設計書_v2.0.md・テーブル定義書_v2.0.md・画面遷移図_v2.0.md（基本設計＝「何を作るか」）を前提に、**「どう実装しているか」**を実装単位（クラス・メソッド・処理順序）まで落とし込んだもの。`docs/コードベース解説.md`（ファイル単位の索引）と合わせて読むと理解しやすい。

---

## 1. 本書の位置づけ

| 既存の設計書 | 扱う粒度 |
|---|---|
| 要件定義書 | 何を実現するか（機能・業務ルール・利用者像） |
| 画面遷移図 | 画面と画面の間をどう移動するか |
| API設計書 | 画面とサーバーの間で、何を・どんな形でやり取りするか |
| テーブル定義書 | データをどう保存するか |
| **本書（詳細設計書）** | **API1本の内部で、どのクラスがどの順番で何を行うか** |

---

## 2. システム構成

```
【ブラウザ】→ frontend（Angular, :4200）→ ―API境界― → backend（Spring Boot, :8080）→ MySQL（eventapp）
```

backendは **Controller → Service → Repository → Entity** の4層構造。frontendは **画面コンポーネント → core/のAPI窓口 → インターセプター** の3層構造。

---

## 3. バックエンド詳細設計

### 3.1 パッケージ構成とクラス一覧

```
com.example.eventapp
├─ entity/       Event, Application, ApplicationStatus, User, Favorite, TicketType, EventComment
├─ repository/   EventRepository, ApplicationRepository, UserRepository,
│                FavoriteRepository, TicketTypeRepository, EventCommentRepository
├─ service/      EventService, ApplicationService, UserService, ReportService, PingService,
│                FavoriteService, EventCommentService
├─ dto/          APIごとの入出力の形（§3.5参照）
├─ controller/   EventController, ApplicationController, UserController, ReportController, PingController,
│                FavoriteController, CommentController
└─ common/
   ├─ AuthInterceptor, AuthContext, CurrentUser, WebConfig
   ├─ exception/   UnauthorizedException, ForbiddenException, NotFoundException, BusinessException, ErrorResponse, GlobalExceptionHandler
   └─ validation/  ValidEventDates, EventDatesValidator
```

### 3.2 クラス図（Entity関連）

```mermaid
classDiagram
  class Event {
    -Long id
    -String name
    -LocalDateTime startAt
    -String place
    -Integer capacity
    -LocalDateTime applicationDeadline
    -String description
    -LocalDateTime deletedAt
    -String organizerName
    -String imageUrl
    -String category
    -String extraQuestion
    -List~TicketType~ ticketTypes
    +isOpen(now) boolean
    +applyChanges(...)
    +softDelete()
    +restore()
  }
  class Application {
    -Long id
    -User user
    -Event event
    -TicketType ticketType
    -String status
    -LocalDateTime appliedAt
    -String extraAnswer
    -LocalDateTime checkedInAt
    +cancel()
    +promote()
    +checkIn()
  }
  class TicketType {
    -Long id
    -Event event
    -String name
    -Integer capacity
  }
  class User {
    -Long id
    -String name
    -String email
    -String role
    +isAdmin() boolean
  }
  class ApplicationStatus {
    <<constants>>
    +ACCEPTED = "受付済"
    +WAITLISTED = "キャンセル待ち"
    +CANCELLED = "キャンセル済"
  }
  class Favorite {
    -Long id
    -User user
    -Event event
  }
  class EventComment {
    -Long id
    -Event event
    -User user
    -String body
    +isOwnedBy(userId) boolean
  }
  User "1" --> "0..*" Application : 申し込む
  User "1" --> "0..*" Favorite : 登録する
  User "1" --> "0..*" EventComment : 投稿する
  Event "1" --> "0..*" Application : 申し込まれる
  Event "1" --> "0..*" TicketType : 区分を持つ
  Event "1" --> "0..*" Favorite : お気に入りされる
  Event "1" --> "0..*" EventComment : コメントされる
  Application "0..*" --> "0..1" TicketType : 区分に紐づく
  Application ..> ApplicationStatus : statusの値
```

### 3.3 主要処理のシーケンス図

#### 3.3.1 ログイン（メールアドレス方式）

```mermaid
sequenceDiagram
  actor U as 利用者
  participant FE as login.ts
  participant API as login-api.ts
  participant C as UserController
  participant S as UserService
  participant R as UserRepository
  U->>FE: メールアドレスを入力して送信
  FE->>API: login(email)
  API->>C: POST /api/login（認証不要）
  C->>S: login(email)
  S->>R: findByEmail(email)
  alt 登録済み
    R-->>S: User
    S-->>C: UserResponse(userId, name, role)
    C-->>FE: 200 OK
    FE->>FE: DummyUserStore.login(userId)
    FE->>FE: role==admin ? /admin/dashboard : /events へ遷移
  else 未登録
    R-->>S: 空
    S-->>C: UnauthorizedException
    C-->>FE: 401「そのメールアドレスは登録されていません」
  end
```

#### 3.3.2 会員登録

```mermaid
sequenceDiagram
  actor U as 利用者
  participant FE as login.ts
  participant API as login-api.ts
  participant C as UserController
  participant S as UserService
  participant R as UserRepository
  U->>FE: 名前・メールアドレスを入力
  FE->>API: register(name, email)
  API->>C: POST /api/users（認証不要）
  C->>S: register(name, email)
  S->>R: existsByEmail(email)
  alt 未登録
    S->>R: save(new User(name, email, "general"))
    S-->>C: UserResponse
    C-->>FE: 201 Created
    FE->>FE: DummyUserStore.login(userId) → /events
  else 登録済み
    S-->>C: BusinessException
    C-->>FE: 400「このメールアドレスは既に登録されています」
  end
```

#### 3.3.3 イベント申込（区分・アンケート・キャンセル待ちの分岐）

```mermaid
sequenceDiagram
  actor U as 一般ユーザー
  participant FE as event-detail.ts
  participant C as ApplicationController
  participant S as ApplicationService
  participant ER as EventRepository
  participant TR as TicketTypeRepository
  participant AR as ApplicationRepository
  U->>FE: （区分があれば選択して）「申し込む」
  FE->>C: POST /api/applications { eventId, ticketTypeId, extraAnswer }
  C->>S: apply(userId, eventId, ticketTypeId, extraAnswer)
  S->>ER: findByIdAndDeletedAtIsNull(eventId)
  alt イベントが無い／削除済み
    S-->>C: NotFoundException
    C-->>FE: 404
  else 受付終了
    S-->>C: BusinessException
    C-->>FE: 400「申込受付は終了しました」
  else 二重申込
    S-->>C: BusinessException
    C-->>FE: 400「すでに申し込み済みです」
  else 申込可能
    alt イベントに区分がある
      S->>TR: findByIdAndEvent_Id(ticketTypeId, eventId)
      alt 区分未指定
        S-->>C: BusinessException
        C-->>FE: 400「区分を選択してください」
      else 区分が見つからない
        S-->>C: NotFoundException
        C-->>FE: 404「指定された区分が見つかりません」
      else 区分が見つかる
        S->>AR: countByTicketType_IdAndStatus(ticketTypeId, "受付済")
        Note over S: 区分のcapacityと比較して受付済／キャンセル待ちを判定
      end
    else 区分なしイベント
      S->>AR: countByEvent_IdAndStatus(eventId, "受付済")
      Note over S: events.capacityと比較して受付済／キャンセル待ちを判定
    end
    S->>AR: save(new Application(user, event, ticketType, status, extraAnswer))
    S-->>C: ApplicationResponse
    C-->>FE: 201 Created
    FE->>FE: /events/:id/done へ遷移
  end
```

#### 3.3.4 申込キャンセル（自動繰り上げ）

```mermaid
sequenceDiagram
  actor U as 一般ユーザー
  participant FE as my-applications.ts
  participant C as ApplicationController
  participant S as ApplicationService
  participant AR as ApplicationRepository
  U->>FE: 「キャンセル」
  FE->>C: DELETE /api/applications/{id}
  C->>S: cancel(userId, applicationId)
  S->>AR: findById(applicationId)
  alt 他人の申込
    S-->>C: ForbiddenException
    C-->>FE: 403
  else 取消不可
    S-->>C: BusinessException
    C-->>FE: 400「取消できません」
  else 取消可能
    S->>S: application.cancel() → status="キャンセル済"
    alt 取消前が「受付済」だった
      alt 区分がある
        S->>AR: findFirstByTicketType_IdAndStatusOrderByAppliedAtAsc(ticketTypeId, "キャンセル待ち")
      else 区分が無い
        S->>AR: findFirstByEvent_IdAndTicketTypeIsNullAndStatusOrderByAppliedAtAsc(eventId, "キャンセル待ち")
      end
      opt 対象がいる
        S->>S: 対象.promote() → status="受付済"
      end
    end
    S-->>C: 204 No Content
    C-->>FE: 一覧を再取得（繰り上げ結果も反映される）
  end
```

#### 3.3.5 マイページのキャンセル待ち順位表示

```mermaid
sequenceDiagram
  participant FE as my-applications.ts
  participant C as ApplicationController
  participant S as ApplicationService
  participant AR as ApplicationRepository
  FE->>C: GET /api/my/applications
  C->>S: myApplications(userId)
  S->>AR: findByUser_IdOrderByAppliedAtDesc(userId)
  loop 各申込について
    alt statusが「キャンセル待ち」かつ区分あり
      S->>AR: countByTicketType_IdAndStatusAndAppliedAtLessThan(ticketTypeId, "キャンセル待ち", appliedAt)
      S->>S: waitlistRank = 件数 + 1
    else statusが「キャンセル待ち」かつ区分なし
      S->>AR: countByEvent_IdAndStatusAndAppliedAtLessThan(eventId, "キャンセル待ち", appliedAt)
      S->>S: waitlistRank = 件数 + 1
    else それ以外
      S->>S: waitlistRank = null
    end
  end
  S-->>C: MyApplicationResponse[]（waitlistRank付き）
  C-->>FE: 200 OK
```

#### 3.3.6 イベント削除（ソフトデリート）・復元

```mermaid
sequenceDiagram
  actor A as 管理者
  participant FE as admin-event-list.ts / admin-deleted-events.ts
  participant C as EventController
  participant S as EventService
  participant ER as EventRepository
  participant AR as ApplicationRepository
  A->>FE: 「削除」→確認ダイアログOK
  FE->>C: DELETE /api/events/{id}
  C->>S: delete(id)
  S->>AR: countByEvent_IdAndStatus(id, "受付済")
  alt 受付済の申込がある
    S-->>C: BusinessException
    C-->>FE: 400「申込があるため削除できません」
  else 無い
    S->>S: event.softDelete() → deleted_atに現在時刻
    S-->>C: 204 No Content
    C-->>FE: 一覧から消える
  end
  A->>FE: 削除済みイベント画面で「復元」
  FE->>C: POST /api/events/{id}/restore
  C->>S: restore(id)
  S->>ER: findByIdAndDeletedAtIsNotNull(id)
  alt 削除済みとして存在しない
    S-->>C: NotFoundException
    C-->>FE: 404「削除済みイベントが見つかりません」
  else 存在する
    S->>S: event.restore() → deleted_at = null
    S-->>C: EventDetailResponse
    C-->>FE: 200 OK（一般・管理者の一覧に再表示される）
  end
```

#### 3.3.7 お気に入り登録・解除（冪等）

```mermaid
sequenceDiagram
  actor U as 一般ユーザー
  participant FE as event-list.ts / event-detail.ts
  participant C as FavoriteController
  participant S as FavoriteService
  participant FR as FavoriteRepository
  U->>FE: 「お気に入り」ボタン
  FE->>C: POST /api/favorites { eventId }
  C->>S: add(userId, eventId)
  S->>FR: findByUser_IdAndEvent_Id(userId, eventId)
  alt 既に登録済み
    S-->>C: 既存のFavoriteをそのまま返す
    C-->>FE: 200 OK（冪等）
  else 未登録
    S->>FR: save(new Favorite(user, event))
    S-->>C: 新規Favorite
    C-->>FE: 201 Created
  end
  U->>FE: もう一度押す（解除）
  FE->>C: DELETE /api/favorites/{eventId}
  C->>S: remove(userId, eventId)
  S->>FR: deleteByUser_IdAndEvent_Id(userId, eventId)
  S-->>C: （存在しなくてもエラーにしない）
  C-->>FE: 204 No Content
```

#### 3.3.8 コメント投稿・削除

```mermaid
sequenceDiagram
  actor U as 利用者（一般・管理者とも）
  participant FE as event-detail.ts
  participant C as CommentController
  participant S as EventCommentService
  participant ER as EventRepository
  participant CR as EventCommentRepository
  U->>FE: コメントを入力して投稿
  FE->>C: POST /api/events/{id}/comments { body }
  C->>S: post(userId, eventId, body)
  S->>ER: findByIdAndDeletedAtIsNull(eventId)
  alt イベントが存在しない
    S-->>C: NotFoundException
    C-->>FE: 404
  else 存在する
    S->>CR: save(new EventComment(event, user, body))
    S-->>C: EventCommentResponse
    C-->>FE: 201 Created
  end
  U->>FE: 自分のコメントを「削除」
  FE->>C: DELETE /api/comments/{id}
  C->>S: delete(userId, commentId, isAdmin)
  S->>CR: findById(commentId)
  alt 投稿者本人でも管理者でもない
    S-->>C: ForbiddenException
    C-->>FE: 403「削除できません」
  else 本人または管理者
    S->>CR: delete(comment)
    S-->>C: 204
    C-->>FE: 204 No Content
  end
```

#### 3.3.9 当日受付（チェックイン）

```mermaid
sequenceDiagram
  actor A as 管理者
  participant FE as admin-checkin.ts
  participant EC as EventController
  participant AC as ApplicationController
  participant S as ApplicationService
  participant AR as ApplicationRepository
  A->>FE: イベント管理から「受付」を開く
  FE->>EC: GET /api/events/{id}/attendees
  EC->>S: listAttendees(eventId)
  S->>AR: findByEvent_IdOrderByAppliedAtAsc(eventId)
  S-->>EC: AttendeeResponse[]
  EC-->>FE: 200 OK（申込者一覧を表示）
  A->>FE: 来場者を選び「チェックイン」
  FE->>AC: PUT /api/applications/{id}/check-in
  AC->>S: checkIn(applicationId)
  S->>AR: findById(applicationId)
  alt statusが「受付済」以外
    S-->>AC: BusinessException
    AC-->>FE: 400「受付済の申込のみチェックインできます」
  else 「受付済」
    S->>S: application.checkIn() → checkedInAtに現在時刻
    S-->>AC: 200 OK
    AC-->>FE: 一覧を再表示
  end
```

#### 3.3.10 イベント登録・編集（区分の全置換）

```mermaid
sequenceDiagram
  actor A as 管理者
  participant FE as admin-event-form.ts
  participant C as EventController
  participant S as EventService
  participant ER as EventRepository
  participant TR as TicketTypeRepository
  participant AR as ApplicationRepository
  A->>FE: イベント情報＋区分（複数可）を入力して「保存」
  FE->>C: POST /api/events （新規）／PUT /api/events/{id} （編集）
  C->>S: create(request) ／ update(id, request)
  S->>ER: save(event) ／ findByIdAndDeletedAtIsNull(id)
  alt リクエストにticketTypesを含めない
    S->>TR: findByEvent_Id(eventId)
    Note over S: 既存区分が有ればcapacityをその合計に同期。無ければ何もしない（区分には一切手を加えない）
    S-->>C: EventDetailResponse
  else リクエストにticketTypesを含める（0件＝区分を無くす場合も含む）
    S->>AR: existsByEvent_IdAndTicketTypeIsNotNullAndStatusIn(eventId, [受付済, キャンセル待ち])
    alt 受付済・キャンセル待ちの申込がある
      S-->>C: BusinessException
      C-->>FE: 400「区分に申込があるため変更できません」
    else 無い
      S->>TR: deleteByEvent_Id(eventId)
      alt キャンセル済の申込の履歴が区分を参照したまま残っている（FK制約違反）
        S-->>C: BusinessException
        C-->>FE: 400「区分に申込の履歴が残っているため変更できません」
      else 削除成功
        S->>TR: save(new TicketType(...)) ×区分数
        Note over S: capacityを新しい区分の合計値に同期
        S-->>C: EventDetailResponse
        C-->>FE: 201（新規）／200（編集）
      end
    end
  end
```

対応：機能11・12／API-06・07／業務ロジック：区分変更時の申込チェック（要件定義書_v2.0.md §8）

### 3.4 認証・共通処理のフロー

すべての `/api/**` リクエストは、Controllerに届く前に必ず `AuthInterceptor`（`HandlerInterceptor`、`WebConfig`で登録）を通る。

```mermaid
flowchart TD
  Req["HTTPリクエスト"] --> Pre{"AuthInterceptor.preHandle()"}
  Pre -->|OPTIONS| Pass1["素通り（CORSプリフライト）"]
  Pre -->|"POST /api/users"| Pass2["素通り（会員登録は認証不要）"]
  Pre -->|"X-User-Idヘッダ無し/不正"| Unauth["UnauthorizedException(401)"]
  Pre -->|検証OK| SetCtx["AuthContext.setCurrentUser()"]
  SetCtx --> Ctrl["Controller"]
  Ctrl -->|"requireAdmin()で一般ユーザー"| Forbid["ForbiddenException(403)"]
  Ctrl -->|正常| Svc["Service → Repository → Entity"]
  Svc -->|"業務ルール違反"| Biz["BusinessException(400)"]
  Svc -->|"対象が存在しない"| NF["NotFoundException(404)"]
  Svc -->|正常| Resp["ResponseEntity（DTOをJSON化）"]
  Unauth --> GEH["GlobalExceptionHandler"]
  Forbid --> GEH
  Biz --> GEH
  NF --> GEH
  GEH --> ErrJson["ErrorResponse（JSON）を返す"]
```

`WebConfig`での適用範囲：`addPathPatterns("/api/**")`・`excludePathPatterns("/api/login", "/api/ping")`。`POST /api/users`だけは`AuthInterceptor`内でメソッド単位に個別許可している。CORSは`WebConfig#addCorsMappings`で`http://localhost:4200`のみ許可。

### 3.5 バリデーション設計

| 対象DTO | 制約 |
|---|---|
| `EventUpsertRequest` | name必須・100文字以内／startAt必須・未来日時／place必須・100文字以内／capacity必須・1以上／applicationDeadline必須／description1000文字以内／organizerName・imageUrl・category・extraQuestionは任意（各文字数上限あり）／ticketTypesの各要素はname必須1〜50文字・capacity必須1以上 |
| `EventUpsertRequest`（クラスレベル） | 申込締切は開催日時**以前**。独自アノテーション`@ValidEventDates` → `EventDatesValidator`が判定し、違反時は`applicationDeadline`フィールドにエラーを紐付ける |
| `LoginRequest` | email必須・メール形式 |
| `UserRegisterRequest` | name必須・100文字以内／email必須・メール形式・255文字以内 |
| `ApplicationCreateRequest` | extraAnswer 0〜500文字。`ticketTypeId`の必須可否はイベントによって変わるため、Bean Validationではなく**Service層で条件付き判定**する |
| `EventCommentCreateRequest` | body必須・1〜500文字 |

**適用タイミングの使い分け**：`EventController`の登録・編集は「`requireAdmin()`（権限チェック）→ `validate(request)`（手動でのValidator実行）」の順。`@Valid`をそのまま使うと権限チェックより先にバリデーションが走ってしまう（一般ユーザーの入力エラーで403ではなく400が先に出てしまう）ため、あえて`@Valid`を使わず手動実行している。login/register/コメント投稿は認証不要または権限分岐が無いAPIのため`@Valid`をそのまま使用。

### 3.6 例外設計

| 例外クラス | HTTPステータス | 発生元の例 |
|---|---|---|
| `UnauthorizedException` | 401 | `AuthInterceptor`（ヘッダ無し・不正なuserId・登録されていないメールアドレス） |
| `ForbiddenException` | 403 | 各Controllerの`requireAdmin()`、他人の申込へのアクセス、コメント削除の権限なし |
| `NotFoundException` | 404 | 存在しないイベント／申込／区分／コメント／削除済みでない復元対象 |
| `BusinessException` | 400 | 定員・締切・二重申込・取消可否・削除可否・メール重複・区分未指定・区分変更不可・チェックイン不可などの業務ルール違反 |
| `MethodArgumentNotValidException`（`@Valid`由来） | 400 | login/register/コメント投稿の入力エラー |
| `ConstraintViolationException`（手動Validator由来） | 400 | イベント登録・編集の入力エラー |
| `HttpMessageNotReadableException` | 400 | 不正なJSON・型不一致 |
| `MethodArgumentTypeMismatchException` | 400 | パスパラメータが数値でない等 |

新しい例外クラスは追加せず、既存8種類の範囲で全業務エラーに対応する（お気に入りの重複・解除は冪等処理のため例外を投げない）。すべて`GlobalExceptionHandler`（`@RestControllerAdvice`）が捕捉し、`ErrorResponse`に統一して返す。

---

## 4. フロントエンド詳細設計

### 4.1 ディレクトリ構成

```
src/app/
├─ core/          API窓口・認証・ガード・インターセプター
├─ login/         SC-01
├─ events/        event-list, event-search, event-detail, apply-done（SC-02系）
├─ my-applications/  SC-03
├─ admin/         admin-dashboard, admin-event-list, admin-deleted-events,
│                 admin-event-form, admin-report, admin-user-list, admin-checkin（SC-04〜06系）
├─ not-found/     404画面
├─ app.ts/html/css  共通ナビ＋router-outlet
├─ app.routes.ts  URL対応表（ワイルドカードルートは必ず配列の最後）
└─ app.config.ts  HttpClient・Routerのプロバイダー登録
```

### 4.2 ルーティング・ガード設計

| ガード | 適用範囲 | 判定内容 | 不許可時の遷移先 |
|---|---|---|---|
| `authGuard` | 全保護ルート | `DummyUserStore.currentUserId() !== null` | `/login` |
| `adminGuard` | `/admin/**` | `currentUserId() === '2'`（管理者の固定ID） | `/events` |

`adminGuard`は`authGuard`の後段として適用する（`canActivate: [authGuard, adminGuard]`の順）。ワイルドカードルート（`**`→`NotFound`）にはガードを適用しない（未ログインでも404自体は表示できる）。

### 4.3 状態管理

- `DummyUserStore`：ログイン中のuserIdを`signal`で保持し`localStorage`にも保存。
- 各画面コンポーネント：一覧・詳細等のデータは`signal`で保持し、テンプレート側は`@if`/`@for`でその`signal`を参照する。

### 4.4 HTTP通信層

```mermaid
flowchart LR
  Comp["画面コンポーネント"] -->|"*-api.tsを呼ぶ"| Svc["core/のAPI窓口"]
  Svc -->|"HttpClient.get/post/..."| I1["dummyAuthInterceptor"]
  I1 -->|"X-User-Idヘッダを付与"| I2["httpErrorInterceptor"]
  I2 -->|送信| BE["backend :8080"]
  BE -->|"レスポンス／エラー"| I2b["httpErrorInterceptor（status===0を正規化）"]
  I2b --> Comp
```

`dummyAuthInterceptor`は未ログイン時はヘッダを付けずに素通りさせる。`httpErrorInterceptor`は「バックエンドに繋がらない」（`status === 0`）場合のみ、固定文言に正規化する。それ以外はバックエンドの`ErrorResponse.message`をそのまま各画面が使う。

---

## 5. Repositoryメソッド一覧（クエリ内容）

| Repository | メソッド | 用途 |
|---|---|---|
| `EventRepository` | `findAllByDeletedAtIsNullOrderByStartAtAsc()` | 一覧取得（削除済み除外） |
| | `findAllByDeletedAtIsNotNullOrderByStartAtAsc()` | 削除済み一覧 |
| | `findByIdAndDeletedAtIsNull(id)` | 詳細・更新・申込時の存在確認 |
| | `findByIdAndDeletedAtIsNotNull(id)` | 復元時の存在確認 |
| `ApplicationRepository` | `countByEvent_IdAndStatus(eventId, status)` | イベント単位の定員チェック・受付済数集計 |
| | `countByTicketType_IdAndStatus(ticketTypeId, status)` | 区分単位の定員チェック |
| | `existsByUser_IdAndEvent_IdAndStatusIn(userId, eventId, statuses)` | 二重申込チェック |
| | `findFirstByEvent_IdAndTicketTypeIsNullAndStatusOrderByAppliedAtAsc(eventId, status)` | キャンセル待ちの繰り上げ対象（区分の無いイベント） |
| | `findFirstByTicketType_IdAndStatusOrderByAppliedAtAsc(ticketTypeId, status)` | キャンセル待ちの繰り上げ対象（区分単位） |
| | `countByEvent_IdAndStatusAndAppliedAtLessThan(eventId, status, appliedAt)` | キャンセル待ちの順位計算（区分の無いイベント） |
| | `countByTicketType_IdAndStatusAndAppliedAtLessThan(ticketTypeId, status, appliedAt)` | キャンセル待ちの順位計算（区分単位） |
| | `findByUser_IdOrderByAppliedAtDesc(userId)` | マイページの自分の申込一覧 |
| | `findByEvent_IdOrderByAppliedAtAsc(eventId)` | 受付画面の申込者一覧 |
| | `findAllByOrderByEvent_StartAtAsc()` | 申込実績CSVの明細順 |
| `UserRepository` | `existsByEmail(email)` | 会員登録の重複チェック |
| | `findByEmail(email)` | ログイン時のユーザー特定 |
| | `findAllByOrderByIdAsc()` | ユーザー一覧 |
| `FavoriteRepository` | `findByUser_IdAndEvent_Id(userId, eventId)` | お気に入りの重複確認・冪等処理 |
| | `deleteByUser_IdAndEvent_Id(userId, eventId)` | お気に入り解除 |
| | `findByUser_IdOrderByCreatedAtDesc(userId)` | マイページのお気に入り一覧 |
| `TicketTypeRepository` | `findByEvent_Id(eventId)` | イベント詳細・編集フォームの区分一覧 |
| | `findByIdAndEvent_Id(id, eventId)` | 申込時の区分存在チェック |
| `EventCommentRepository` | `findByEvent_IdOrderByCreatedAtAscIdAsc(eventId)` | イベント詳細のコメント一覧（created_atが同一秒の場合に備えidを第2キーにする） |

---

## 6. 設計書間トレーサビリティ

| 本書の章 | 対応する既存設計書 |
|---|---|
| 3.3.1〜3.3.2（ログイン・会員登録） | API設計書 API-10・11／要件定義書 機能1・2 |
| 3.3.3〜3.3.5（申込・キャンセル・順位） | API設計書 API-03・04・05／要件定義書 §8 業務ルール |
| 3.3.6（削除・復元） | API設計書 API-08・13・14／要件定義書 機能13・14 |
| 3.3.7（お気に入り） | API設計書 API-15〜17／要件定義書 機能17 |
| 3.3.8（コメント） | API設計書 API-20〜22／要件定義書 機能18 |
| 3.3.9（チェックイン） | API設計書 API-18・19／要件定義書 機能19／画面遷移図 SC-06 |
| 3.3.10（イベント登録・編集） | API設計書 API-06・07／要件定義書 機能11・12・§8 区分変更時の申込チェック |
| 3.4（認証・例外） | API設計書 §0 共通事項 |
| 4.2（ルーティング・ガード） | 画面遷移図 §4 補足メモ |
| 5（Repositoryメソッド） | テーブル定義書 §3 インデックス一覧 |
