# F-2　業務シナリオ手動実行　シナリオ結果

実施日：2026-09-15
方法：要件定義書_v1.0.md §5「業務シナリオ（正常系）」の4本を対象に、F-1（結合・動作確認）で実施した一連の操作結果、
および D-8（API手動疎通確認）でのAPI単体確認結果と突き合わせて実行結果を確認した。

## 【一般】シナリオ1：イベントに申し込む

| 手順 | 期待する動作 | 結果 |
|---|---|---|
| 0. ログイン | 一般ユーザーとしてログインできる | OK（F-1手順2） |
| 1. 一覧→詳細 | 開催日時・定員・残り枠・申込締切が詳細画面に表示される | OK（F-1手順3、`EventDetailResponse`にremaining等を含む） |
| 2. 申し込む | 「申し込む」ボタンでAPI-03が呼ばれる | OK（F-1手順4） |
| 3. 完了画面 | ステータスが「受付済」と表示される | OK（申込完了画面が申込APIのレスポンスをそのまま表示） |

## 【一般】シナリオ2：申込状況の確認と取消

| 手順 | 期待する動作 | 結果 |
|---|---|---|
| 0. ログイン | 一般ユーザーとしてログインできる | OK |
| 1. マイページ | 自分の申込一覧（受付済／キャンセル済）が表示される | OK（F-1手順6） |
| 2. 取消 | 取消後にステータスが「キャンセル済」に変わる | OK（F-1手順7） |

## 【管理者】シナリオ1：イベントを登録・編集・削除する

| 手順 | 期待する動作 | 結果 |
|---|---|---|
| 0. ログイン | 管理者としてログインできる | OK（F-1手順9） |
| 1. 新規登録 | イベント名・開催日時・定員・申込締切・場所・説明を入力して登録できる | OK（F-1手順10） |
| 2. 一般向け一覧への反映 | 登録したイベントが一般ユーザー向け一覧に表示される | OK（D-1/D-2でAPI-01・API-06が同じeventsテーブルを参照することを確認済み。GET /api/eventsは全ユーザー共通で、登録直後から一覧に含まれる） |
| 3. 編集 | 内容（定員など）を編集・保存できる | OK（F-1手順11） |
| 4. 削除 | 削除後、一覧から消える | OK（F-1手順12、D-8でも確認済み） |

## 【管理者】シナリオ2：申込状況の把握と実績出力

| 手順 | 期待する動作 | 結果 |
|---|---|---|
| 0. ログイン | 管理者としてログインできる | OK |
| 1. 受付済数・定員・充足率の確認 | 申込状況・実績画面に表示される | OK（F-1手順13、`admin-report`画面がAPI-09のJSON集計を表示） |
| 2. 申込数の多い順ソート | 並び替えで人気イベントが分かる | OK（D-8でAPI-09の`sort=accepted_desc`がacceptedCount降順で返ることを確認済み。画面側は`admin-report.ts`の`changeSort()`がこのクエリパラメータを切り替えるのみ） |
| 3. CSV出力 | 申込データ（イベント名・申込者名・申込日時・ステータス）がCSVでダウンロードできる | OK（D-8でAPI-09の`format=csv`がBOM付きUTF-8・正しい列でダウンロードされることを確認済み。画面側は`admin-report.ts`の`downloadCsv()`がBlobを受け取り`<a download>`で保存するのみ） |

## 総括

- 一般シナリオ1・2、管理者シナリオ1の全手順はF-1で実際にブラウザ操作して確認済み。
- 管理者シナリオ2のうち「ソート」「CSV出力」の2点はF-1のブラウザ操作では未実施だったが、D-8でAPI-09の該当機能（`sort`パラメータ・`format=csv`）を直接検証済みであり、画面側はそのAPIをそのまま呼び出すだけの薄い実装（`admin-report.ts`）であるため、機能として問題ないことを確認した。
- 4本すべてのシナリオが要件定義書の期待通りに動作することを確認した。
